spring:  
  application:
    name: hrConfigEurekaClient
  cloud:
    config:
      uri: http://localhost:8888
      
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
      

turbine:
  appConfig: EurekaServer,hr
  aggregator:
    clusterConfig: WEB,RACES